{
  "name": "swrp",
  "version": "1.0.0",
  "description": "Star Wars Roleplay Game — Web App",
  "scripts": {
    "parse:xlsx": "node scripts/parse-xlsx.js",
    "build:data": "node scripts/build-game-data.js",
    "build": "npm run parse:xlsx && npm run build:data",
    "serve": "npx --yes serve public -l 3000"
  },
  "license": "ISC",
  "devDependencies": {
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
